---
founder:
  dumpPath: '${ERIN_DUMP_PATH}'

healthcheck:
  listen: ':9000'

log:
  tag: kannel.erin
  level: debug
  output: stdsep
  format: json
  formatConfig:
    colors: false
    disableTimestamp: true
